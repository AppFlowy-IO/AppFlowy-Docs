type: object
properties:
  pre_hash:
    type: string
    description: >
      A string value that that will be used to calculate the hash of the row, which will become the row's unique identifier.
  cells:
    type: object
    description: >
      The cells of the database row.
      {
        "key": value,
        ...
      }
      key: field_id or field_name
      value: depends on the field type (Server will attempt to convert the value to the field type)
  document:
    type: string
    description: >
      The document content of the database row, in Markdown format.
